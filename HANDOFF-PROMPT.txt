Работаем над studyroom-project-site. Сначала прочитай память:
D:\Эдгар, (НЕ ТРОЖ ОПАСТНО ДЛЯ ЖИЗНИ)\cursor projects\claude projects\memory\MEMORY.md
и статус: ...\memory\project_studyroom_status.md, правила: ...\claude projects\CLAUDE.md.
Память только на D:, на C: не сохранять. По-русски на «ты», коротко, сначала план/баги — потом фикс.

Проект: D:\Эдгар, (НЕ ТРОЖ ОПАСТНО ДЛЯ ЖИЗНИ)\cursor projects\claude projects\projects\studyroom-project-site
Ветка main → push в main = автодеплой Cloudflare Pages (deploy.yml). Сборка: cd site && npm run build (exit 0, 807 стр.).
status.json теперь в .gitignore и НЕ в git (генерится на каждом npm run build) — проблема rebase снята.
НЕ пушить/НЕ удалять данные каталога без моего слова. Удаление файлов — только после показа списка и моего «да».
Перед push: git fetch — CI staggered-scrape и он-деманд ПАУК пушат сами (rebase поверх, данные каталога не клобить).
В конце commit: Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>

=== СДЕЛАНО 2026-06-08, СОРОКА ДОБИТА (всё СМЁРДЖЕНО в main) ===
СОРОКА follow-up A/B/C — PR #32/#33/#34 (панель source-aware, 2 IELTS-фикса, alias-карта 172 вуза).
- PR #36 (merge 18bab5d) — ВАРИАНТ 1: liveCheckUni добирает цены для tuition_zero с офсайта
  (валюта вуза + tuitionPlausible; авто-филл byProgram ТОЛЬКО при корроборации >=2 URL и conf>=0.85,
  иначе кандидат оператору в панель: case.official, decision=null). soroka-apply.mjs: decision=update
  разрешён и для tuition_zero. Прогон 13 нулевых: lsu → 3 PhD-кандидата ~12886 USD; остальные
  no-official-site/no-money-found (офсайты не отдают цен на мелких путях — потолок варианта 1).
  + ФИКС БАГА: вуз со слагом `constructor` (Constructor Univ) ронял полный прогон —
  SLUG_ALIASES['constructor'] возвращал Object.prototype.constructor (не массив). Guard Array.isArray
  в soroka.mjs И merge-programs.mjs. Скоуп-раны (--slug) баг НЕ ловили, только полный проход.
  + ПОЛНЫЙ ПРОГОН СОРОКИ (804 вуза): 772 кейса в /manager (было 355) — alias-карта закрыла дыры:
  tuition_mismatch 420 (было ~9), outlier 280, zero 64, currency 8; 4132 corroborated (было 3129).
  Каталог: только метаданные-провенанс в 97 вузах (данные не тронуты). Build 807/exit 0.
- PR #37 (merge 51b841b) — СОРОКА ВСТРОЕНА В CI: дыра была — pauk.mjs имеет Phase 4.5 (soroka),
  но pauk.mjs в CI НИКТО НЕ ЗОВЁТ (воркфлоу гоняют коллекторы+merge напрямую; РЕВИЗОР в staggered
  есть, СОРОКА нигде). Добавлен шаг СОРОКА в scrape-monthly.yml после merge, --skip-live
  (детерминированно, $0). review.json пушится через `git add site/public/api/`, catalog-метаданные
  не коммитятся (без месячного churn). Теперь полный кросс-чек идёт сам 1-го числа. Точечный live — on-demand.

=== СДЕЛАНО 2026-06-08, KAPLAN ПОРТИРОВАН (ветка feat/kaplan-merge-port ЗАПУШЕНА, PR не открыт) ===
Находка: Kaplan был FOUNDING-коллектором (TS scraper/src/cli.ts, прямая запись в каталог, ~29 вузов),
но осиротел от merge-пайплайна и CI (слот kaplan-pathways гонял чужой scrape-direct-partners-v2).
Порт (коммиты ca6d490 спека + b2a40c0 фича):
- НОВЫЙ scraper/scrape-kaplan-all.mjs: фид `degree_finder_object` (один fetch = вся база Kaplan,
  БЕЗ Playwright, $0). 25 вузов/4732 программы, у каждой реальная цена+валюта. Матчинг института
  фида → слаг каталога по токенам имени (токены вуза каталога ⊆ имени института); кампусы (ASU) схлопнуты.
- merge-programs.mjs РАСШИРЕН: перенос tuition из extracts (fill-null по ФИНАЛЬНОМу слагу через temp-поле
  _feePerYear, НЕ перезаписывает; валюта вуза — если пуста). Раньше merge цены НЕ нёс вообще. Выгода всем
  коллекторам. Проверено glasgow: 0 утечек temp-полей, программы целы, существующие цены целы.
- revizor.mjs: SCHEDULE_TO_DOMAIN_KEY (kaplan-pathways→kaplan, navitas-pathways→navitas). Staggered
  зовёт ревизор ключом расписания, а домены под коротким ключом — иначе «running on all» (терял скоуп).
  Проверено: revizor --aggregator=kaplan-pathways аудитит РОВНО 29 Kaplan-вузов.
- scrape-staggered.yml: слот kaplan-pathways → scrape-kaplan-all.mjs. registry.json: status=done.
  cli.ts: маркер legacy (НЕ удалён — оставлен ради accommodation/scholarships/описаний, merge их не несёт).
- Build 807/exit 0 на смёрдженном каталоге. Каталог НЕ коммичен (CI регенерит из kaplan-extracts
  детерминированно на ближайшем staggered+merge). 25 kaplan-extracts закоммичены. Спека:
  docs/superpowers/specs/2026-06-08-kaplan-merge-port-design.md.
СЛЕДУЮЩЕЕ по Kaplan: открыть/смёрджить PR ветки feat/kaplan-merge-port.

=== i18n /en — ОТМЕНЁН (2026-06-08, решение владельца) ===
Сайт остаётся на ОДНОМ языке (русском). Полный i18n НЕ делаем (804 вуза прозы — слишком много).
Позже — только перевод ЛЭНДИНГ-страниц на английский. Не предлагать полный i18n снова.

=== СЛЕДУЮЩЕЕ (бэклог СОРОКИ/каталога) ===
- 772 кейса в /manager ждут ручных решений владельца (apply → soroka-apply.yml кнопкой в панели).
- Открыть/смёрджить PR feat/kaplan-merge-port (Kaplan порт).
- Перевод лэндинг-страниц на английский (позже, НЕ весь каталог).
- Точечный live-добор tuition_zero для конкретных вузов — `node scraper/soroka.mjs --slug=X` (без --skip-live).

=== СДЕЛАНО 2026-06-05 (PR #30 СМЁРДЖЕН в main, merge 8a26b99) ===
- НОВЫЙ агент СОРОКА (scraper/soroka.mjs + scraper/lib/numbers.mjs) — директор достоверных цифр:
  tuition программ, цены жилья из текста, keyFacts-цифры, IELTS/GPA. Phase 4.5 в pauk.mjs (--skip-soroka).
- 3 прохода: кросс-сверка extracts ($0; extracts в ДВУХ корнях — scraper/sources И корневой sources)
  → санити-диапазоны по валюте ($0) → живой офсайт ТОЛЬКО для спорных (кап --max-live=25).
- Гибрид: авто-применение только official-подтверждённых >=0.85; остальное → кейсы
  site/public/api/soroka-review.json (формат item = РЕВИЗОР) + sources/audit/soroka-report.json.
- Прогон 804: 19732 факта, 3129 corroborated (confidence 0.8 в 91 файле), 355 кейсов:
  64 tuition=0, 280 outliers, 9 mismatch (domus-academy ~18% vs edvoy), 2 IELTS-бага
  (asc-perth=0, queen-ethelburgas=1). Build 807/exit 0, validate 804/804.
- Схемы: confidence?:0..1 в program/scholarship/campus/accommodation (scraper schema.ts + validate-unis.ts).
- Дизайн-спека: docs/superpowers/specs/2026-06-05-soroka-design.md.

=== ЗАДАЧИ СОРОКА follow-up (приоритет на следующую сессию) ===
🟡 A. Wiring кейсов СОРОКИ в панель manager + revizor-apply (формат уже совместим с РЕВИЗОРОМ;
   паттерн — блок revizor в manager.astro ~1676+). 355 кейсов ждут ручных решений.
🟡 B. Разобрать кейсы: 64 tuition=0 (цена не заполнена — добить из extracts/офсайтов),
   2 IELTS-бага — быстрые фиксы руками.
🟢 C. Маппинг слагов extract<->catalog: abertay (каталог) vs abertay-university (extract) —
   такие вузы НЕ кросс-сверяются (та же дыра в merge-programs). Построить alias-карту.

=== СДЕЛАНО 2026-06-04 (ВСЁ ЗАПУШЕНО, последний fae5729 + CI-коммиты ПАУКа) ===
- Мёртвый код: сайт 7 файлов (b8c55d7) + скрейпер 10 файлов по графу (cc4d157).
- sourceHash: 140 JSON placeholder → sha256(slug|sourceUrl) (c578dd0); qs-build-prompt — формула.
- verify-on-demand.yml: shmel soft, generate-verify soft + гейт-шаг (b184e95).
- Конфликт-маркеры PR #18 (06dee90, 29.05) разрешены в 5 файлах (c5c320a).
- dotenv-фикс (fae5729): scraper/lib/load-env.mjs (ручной .env-лоадер) в edvoy/studygroup/qs/iapro;
  dotenv НЕ зависимость, его импорт валил коллекторы в CI (маскировал || true).
- ПАУК ПРОВЕРЕН живым раном 26941327251: Edvoy 408 вузов/44147 программ/0 ошибок, QAHE ок,
  build+deploy ок. iapro — TimeoutError на логине (нет кредов, известный блокер).
- Параллельная сессия: push-retry во всех 9 push-воркфлоу (2c18857) — лечит race ПАУК vs revizor-apply.

=== СДЕЛАНО 2026-06-05, сессия 2 (PR #29 f5fc42cf + PR #31 f7e0ffa, ОБА СМЁРДЖЕНЫ) ===
Все 5 автономных задач 06-04 закрыты:
- CI Node 24: все 10 воркфлоу checkout@v5 / setup-node@v5 (дедлайн 16.06 снят).
- Стипендии: КОРЕНЬ — Edvoy API имён НЕ отдаёт; scholarshipTag = UI-бейдж «Scholarship applied!»
  (57/57 одинаковых). StudyGroup стипендии вообще не собирал. Гейт работал верно. Фикс:
  scrape-edvoy-all пишет флаг scholarshipsAvailable (не junk-массив), bobr-merge фильтрует
  junk из стейл-extracts. РЕШЕНИЕ: кампусы Edvoy НЕ гейтить (структурные GraphQL-поля,
  гейт зарезал бы все — задокументировано в mergeEdvoy). −45 легаси «Scholarship applied!»
  из каталога (одобрено владельцем). Реальные стипендии = discovery через ШМЕЛЯ (official
  tier 0.55 проходит гейт) — отдельная фича, в бэклог.
- Фото-зоопарк: граф по всем 72 скриптам → удалено 28 мёртвых (−10к строк): 6 фото
  (pilot-photo-sets, download-logos, photos-from-manifests, discover-logos, visual-check,
  bobr-rehero) + 9 seed-* + 8 overnight-цепочка + 5 старых поколений. ОСТАВЛЕНЫ 6 ручных:
  qs-build-prompt, smoke-test-all, bobr-serve, bobr-preview, discover-studygroup,
  bobr-strip-topup (решение владельца: не используется ≠ удалять). Целостность проверена:
  registry 16/16 OK, синтаксис агентов OK, битая ссылка smoke-test→seed-navitas-au убрана.
- Техдолг: status.json в .gitignore + untracked (build сам генерит через generate-status.mjs);
  sources/revizor-flags.json (стейл-дубль 24.05) удалён — канонический scraper/sources/;
  wrangler@latest → wrangler@4; 4 стейл-лога scraper-*.log из корня стёрты.
- Проверка №5: бага НЕТ. Живой /api/status.json: iapro dayOfMonth=21; manager.astro
  aggregatorNextRunFromDayOfMonth(21) при 05.06 → 21 июня 2026. «1 июля» был бы только
  при отсутствии dayOfMonth.

=== ЗАДАЧИ (по приоритету) ===
🟡 СОРОКА follow-up A/B/C — СДЕЛАНО (см. блок СОРОКА выше); 772 кейса ждут решений владельца.
✅ Kaplan — ПОРТИРОВАН в merge-пайплайн (см. блок KAPLAN выше). i18n — ОТМЕНЁН.
⏸️ Стипендии с официальных сайтов через ШМЕЛЯ (см. выше).

=== НУЖНО ОТ ВЛАДЕЛЬЦА (разблокирует прод, без изменений) ===
1) contacts.ts: телефон / WhatsApp / Telegram / email / БИН / адрес.
2) Видео-отзывы: YouTube (unlisted) → STUDYROOM_REVIEWS (static.ts), секция reviews в [slug].astro.
3) Cloudflare Pages env: CRM_WEBHOOK_URL (+ опц. CRM_TYPE, LEAD_NOTIFY_URL) — без него формы отдают ошибку.
4) og:image — дизайн-ассет 1200×630.
5) IAPro: рабочие логин/пароль (текущие секреты есть, но логин-форма Salesforce таймаутит —
   проверить креды или селекторы).
6) Хардкод #requirements [slug].astro:719–729: GPA «4.0+» и тиры IELTS — UK-pathway питч на всех
   странах. Решение по копирайту, потом чинить.

=== КОНТЕКСТ ===
Confidence-гейт: scraper/lib/confidence.mjs, MIN=0.85, scoreFact = official .55 / aggregator .35
/ inferred .15 + quality*.30 + corroborated .15 + linkLive .10. В каталог только >=0.85, ниже → аудит
sources/audit/*-lowconf.json (обогащать, не резать — ничего не удалять).
Расписание CI: dayOfMonth в scraper/sources/aggregator-schedules.json (cron статичен, ручной синк).
Registry агентов: scraper/tasks/registry.json (ПАУК=pauk.mjs программы, БОБР=bobr.mjs кампусы/жильё,
ОРЁЛ=orel.mjs фото, РЕВИЗОР=revizor.mjs+revizor-apply, ШМЕЛЬ=shmel.mjs верификатор).
.env-лоадер коллекторов: scraper/lib/load-env.mjs (не возвращать dotenv — его нет в зависимостях!).
