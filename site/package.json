{
  "name": "studyroom-site",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "StudyRoom — catalog hub + per-university landings (Astro). Schema in Stage 1, app in Stage 2.",
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/generate-status.mjs && astro build",
    "generate:status": "node scripts/generate-status.mjs",
    "preview": "astro preview",
    "check": "astro check",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^5.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "wrangler": "^4.94.0"
  }
}
