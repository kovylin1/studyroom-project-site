# Передача смены — 2026-08-03, вечер

> Предыдущая передача (`HANDOFF-2026-08-03.md`) закрыта целиком. Это следующий шаг.
> SSOT задач — `BACKLOG.md`.

---

## Промпт

```
Проект: studyroom-project-site (D:\...\claude projects\projects\studyroom-project-site).
Ветка feat/kompas влита в main (PR #57, main = 9298ef52), рабочее дерево чистое.
Читай BACKLOG.md (IN PROGRESS + TODO) и этот файл. Работай в QUIET_MODE.md.

Задача: 24 новых вуза QS, вариант Б — город И длительность с офсайтов.
Владелец выбрал Б осознанно, зная, что это коллектор на домен и несколько заходов.
Сперва отсеять мусор из списка (см. ниже), потом мерить разметку, потом писать разбор.
```

---

## Состояние

`main` = `9298ef52`, задеплоено. Три пункта прошлой передачи закрыты, четвёртый
(757 осиротевших решений) разобран и оказался пустым — см. ниже.

Живой каталог за обе сессии не изменён ни разу.

## Что закрыто в эту сессию

| Коммит | Что |
|---|---|
| `02c5627b` | Navitas: сидер больше не раздаёт выдуманные цены и не стирает каталог |
| `0e530bf8` | Пересчёт `direct-review.json` + свёртка кейсов прайса + панель перестала стирать решения |
| `00d38b31` | Голый `$` читается по стране вуза |
| `01a1d9ec` | BACKLOG |

**757 осиротевших решений — применять было нечего.** Флаг `applied: false` врал:
в поле `decisionNote` самих кейсов записано «Решение 2026-07-29: в каталог записана
цена источника (kompas-adopt-fees --threshold=2, 1720 цен у 98 вузов)». Скрипт цены
записал, а флаг не проставил. Сверка с живым каталогом: 729 совпадают до рубля,
расхождений 0; ещё 18 — кейсы-сводки без своей программы, 10 — решения «пропустить».
Разбор: `scraper/kompas-orphan-decisions-audit.mjs`, разряды лежат в
`sources/kompas/panel-orphan-decisions.json` (поле `orphanVerdict`).
**Урок:** `applied` в кейсах не доказательство. Сверять с каталогом.

## Задача следующей сессии — 24 вуза QS, вариант Б

Владелец 2026-08-03: «давай скорее вариант Б» — город И длительность с офсайтов,
не выводить длительность из уровня.

### Сперва отсеять: вузов там не 24

Список — `sources/kompas/QS-NEW-CARDS.md`, раздел «новый вуз — 24». Просмотр глазами:

- **`Testing December - Testing purpose`** (Украина, INTO, 2 программы) — тестовая
  запись портала QS. Не вуз, заводить нельзя.
- Не университеты: `St. James Catholic Middle School` (1), `Wycombe Abbey International
  School` ×3 (школы, 2+2+3 программы), `ILAC International Language Academy of Canada`
  (языковая школа, 1), `MPW` (4), `On Campus Ireland` (4), `UCL Centre for Languages &
  International Education` (11 — это подразделение UCL, а не вуз).
- Почти все программы сидят в первой пятёрке: **UWE 352, Falmouth 124, Worcester
  (Undergraduate) 113, Kwantlen Polytechnic 107, Worcester (Postgraduate) 99** — 795 из 1118.
  Worcester приходит двумя записями на одну карточку: сводить ОБЪЕДИНЕНИЕМ (урок QS-5).

Порядок работы напрашивается такой: сначала эта пятёрка, потом остальные настоящие вузы,
мусор — отдельным списком владельцу на подтверждение.

### Что уже известно про данные

- Выгрузки QS: `sources/kompas/extracts/qs/*.json`, 512 штук.
  Поля записи: `slug, name, source, sourceUrl, aggregator, access, country, provider,
  partnerType, campusesStated, educationLevels, internationalStudents, yearlyFeesStated,
  ownership, foundedYear, description, catalogSlug, matchMethod, feeNote, programs`.
  Поля программы: `title, level, sourceLevel, degreeGroup, programType, stem,
  applicationFee, applicationFeeCurrency, intakeNote, campuses, tuition, currency,
  feeBasis, campusCosts, programUrl, raw`.
- **Города нет ни в одном поле.** `campusesStated` — строка «1 Campus». Но не проверено,
  что даёт `description` и `campuses` у программ: там могут стоять названия кампусов
  с городом («Bay Campus», «Frenchay Campus»). **Проверить это первым делом — дёшево
  и может снять часть сетевой работы.**
- **Адреса офсайта в выгрузке QS тоже нет.** Есть `scraper/discover-official-sites.mjs`
  — с него и начинать поиск домена.
- Разбор 133 непривязанных: `sources/kompas/qs-unlinked.json`, поле `verdict`
  (`absent` 116, `country-mismatch` 9, `resolved` 5, `ambiguous` 3) и `qs-unlinked-report`.

### Требования схемы, из-за которых карточки не заводятся

`site/src/schema/university.ts`: `city` — непустая строка, `durationYears` — положительное
число у КАЖДОЙ программы, `programs` — минимум одна, `tuition.byProgram` —
`record(slug, number >= 0)`. Проставить город и длительность от себя = фабрикация.

### Порядок по правилам проекта

1. По каждому домену **сперва замерить разметку одной страницы курса глазами**, потом
   писать разбор (правило из плана КОМПАСа, задача 1, пункт 2).
2. Длительность брать со страницы курса. Если её там нет — **не выводить из уровня**,
   а заводить кейс в `/manager` (владелец выбрал Б именно ради этого).
3. Сетевой прогон — Node-скриптом в фоне по `QUIET_MODE.md`, не поштучно агентами.

## Хвост, который не обсуждали

- **4 карточки QS с двумя записями портала** — `naba-milano`, `rit-dubai`, `tamucc`,
  `uvic`. Программы сводить ОБЪЕДИНЕНИЕМ (урок QS-5).
- **Панель: 4940 кейсов, 3800 ждут оператора.** Из них 1361 — сверка QS, 2065 — прайсы
  прямых партнёров (уже сведены по сумме, дальше сводить некуда).
- **4 вуза без программ у самого портала QS**: American Collegiate Washington DC,
  Centennial College, Durham College, Heriot-Watt Malaysia Foundation.
- **Суммы стипендий**: правило «голый `$` по стране» починено, применится на
  `kompas-collect-scholarships.mjs --refetch` (сетевой прогон) — 118 записей из 453.
- **`officialUrl` нет у 204 карточек.**

## Гигиена

- GateGuard режет первую команду сессии и все «разрушительные» (включая `git restore`).
  По правилу 1 из `CLAUDE.md` его можно выключить на сессию: `ECC_GATEGUARD=off`.
- **Эта сессия стоила ~$92.** Дорого вышло: чтение больших исходников целиком
  (`seed-navitas-uk.mjs` — 49k токенов за один Read) и пошаговые правки через Edit там,
  где хватило бы одного скрипта. Читать большие файлы кусками через Grep, а не Read.
- `npm test` в scraper гоняет vitest, а тесты написаны на `node:test`. Проверять
  `node --test scraper/*.test.mjs scraper/lib/*.test.mjs` — сейчас 133 зелёных.
