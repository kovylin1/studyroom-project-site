# План работ по каталогу — состояние на 2026-08-20

Задачи разбиты на три категории по одному признаку: **где расхождение**.
У каждой свой номер — на него можно ссылаться («делаем 2.3»).

- **Категория 1** — есть у агрегатора, нет в каталоге. Новые вузы.
- **Категория 2** — есть в каталоге, нет у агрегатора. Непроверяемое.
- **Категория 3** — всё остальное: цены, стипендии, полнота, запуск.

Ветка `feat/kompas-qs`, на 168 коммитов впереди `main`. **`main` последний раз
трогали 18 июня**, поэтому ничего из перечисленного на живом сайте пока нет.
Мерж в `main` = деплой, это отдельное решение (задача 3.16).

Счёт состояний на 23.08: каталог **887 карточек**; выгрузок QS привязано 505 из 512,
не привязано 5. Было на 20.08: **853 карточки / ~91 850 программ**; рабочая копия
`catalog-work` — 823 карточки / 106 033 программы (накоплено КОМПАСом, не применено).
Панель `/manager`: ~5136 кейсов, около 3300 открыто.

---

# Категория 1 — есть у агрегатора, нет в каталоге

Источник — 133 непривязанные записи QS (`sources/kompas/qs-unlinked.json`),
очередь — `sources/kompas/NEWCARDS-QUEUE.md`.

## Сделано 2026-08-20 (коммит `c3423f00`)

- [x] **1.1 Заведение новых карточек QS.** 30 карточек / 639 программ.
      Каталог 823 → 853. Из них 26 собраны из выгрузок QS, 4 взяты готовыми
      черновиками сессий 03–04.08 (Falmouth 93, Worcester 108, MPW 2, PHBS 2).
      KPU, Marshall и Mercy, которые были мертвы из-за отсутствия срока, заведены.
- [x] **1.2 Привязка 6 выгрузок к живым карточкам.** 481 программа:
      `uwe-bristol` 352, `srh-germany` 67, `luiss` 32, `glion-switzerland` 15,
      `ucl` 11, `cesi` 4. Вердикт `absent` по ним был багом разбора.
- [x] **1.3 Удаление служебных записей портала.** «Testing December» и
      «Indian Internal Applications» — 40 программ, не учебные заведения.
- [x] **1.4 Срок программы стал необязательным.** QS не отдаёт `durationYears`
      ни у одной из 1318 программ. На странице — «срок уточняется».

## Открыто

- [ ] **1.5 Шесть карточек ждут города — 21 программа.**
      Города нет ни в QS, ни в Wikidata. Назови — заведутся одним прогоном
      (`node scraper/kompas-newcards-build.mjs --apply`).
      | Карточка | Программ | Почему не нашлось |
      |---|---:|---|
      | `university-of-tasmania-international-pathway-college` | 7 | Wikidata отдаёт штат «Tasmania» |
      | `oxford-international-education-group-english-schools` | 2 | шесть площадок в трёх странах |
      | `oxford-international-education-group-ielts-and-tesol` | 2 | то же |
      | `oxford-international-education-group-junior` | 1 | то же |
      | `university-bridge` | 5 | Wikidata нашла мост в Саскачеване |
      | `on-campus-ireland` | 4 | сущности в Wikidata нет |

- [x] **1.6 Подтвердить 20 «надёжных» привязок QS** — сделано 21.08 вместе с остальной
      привязкой (64 выгрузки / 1591 программа). Разбор — `sources/kompas/CAT1-REVIEW.md`.

- [ ] **1.7 Разобрать оставшиеся привязки.** После прогона 21.08 осталось 45 выгрузок /
      1294 программы; они сведены в четыре вопроса владельцу в `sources/kompas/CAT1-REVIEW.md`:
      новая карточка (12 вузов, 550 прог), кампус сети (9), правило для подготовительных
      отделений (16, 509 прог), SRH (1).
      Схожесть 0.5–0.83, ошибка тихо подмешала бы чужие программы — поэтому
      автоматом не привязывается: «University of Oklahoma» → `into-oklahoma`
      это вуз против pathway-центра при нём, а не одно и то же.

- [ ] **1.8 Кейсы `kompas_qs_link` — 53 → 25 после привязки 21.08.** Все 25 входят
      в те же четыре вопроса `CAT1-REVIEW.md`, отдельной работы не требуют.
      Разбор — `sources/kompas/qs-relink-review.json`.

- [ ] **1.9 Добор программ `kompas_programs_missing` — 207 → 239 после пересверки 21.08.**
      Есть у источника, нет в карточке. Рост — следствие привязки: источник стал
      виден, программы не перенесены. В основном без распознаваемого уровня;
      уровень не выдумываем.

- [ ] **1.10 Хвосты по заведённым карточкам:**
      - 171 цена у 16 карточек отброшена как не сходящаяся с годовым диапазоном
        (`newcard_price_out_of_range` в `sources/kompas/newcards/build-cases.json`);
      - 66 строк QS Worcester без пары — расхождения по существу;
      - 43 варианта курса Falmouth со спорным сроком: «3 years / 4 years».
        Утвердишь правило «вариант = база + 1 год» — применится ко всем разом;
      - цена PHBS 29 500 GBP — это за всю программу (2 года). Если поле годовое,
        ставить 14 750;
      - 49 программ Worcester без срока (MPhil/PhD) — теперь их можно завести
        с пометкой «срок уточняется», раньше отбрасывались.

- [ ] **1.11 Пекинский университет** — у QS только бизнес-школа. Головная
      карточка требует своего сбора с `pku.edu.cn`, по объёму это уровень Falmouth.

---

# Категория 2 — есть в каталоге, нет у агрегатора

- [ ] **2.1 109 карточек не найдены ни у одного источника — 3 700 программ.**
      Главный вопрос категории: откуда они взялись и что с ними делать.
      Топ: Edith Cowan Sri Lanka 336, Oxford 212, Catholic University of America 191,
      Western Ontario 116, Simon Fraser 112, Toronto Met 107, Wilfrid Laurier 107,
      Victoria 99, Manitoba 98, SRH International College 82.
      Профиль — Канада, континентальная Европа, Турция, Австралия.
      *Варианты:* оставить · пометить «источник не подтверждён» · убрать с сайта ·
      искать под них новый агрегатор.
      Данные: `sources/kompas/coverage-report.json`, поле `outsideePartnerList`.

- [ ] **2.2 59 карточек «источник молчит» (`kompas_no_extract`) — 2 791 программа.**
      Вуз размечен партнёром, источник собран целиком, но выгрузки по нему нет.
      Это не дыра сбора, а тот же баг привязки, что в 1.7. **Закрывается тем же
      движением — делать вместе с 1.7.**
      Топ: St. Francis Xavier 189, UNC Wilmington 186, RIT Dubai 173,
      Queen Mary Malta 149, Trine 148, Webster Vienna 147, Queens College CUNY 138.

- [ ] **2.3 129 карточек с «лишними» программами без метки — 9 717 программ.**
      P1 метил каталог, когда QS был заблокирован, и до этих карточек не дошёл.
      Нужен повторный прогон, но сперва `kompas-inventory.mjs --write-copy`:
      рабочая копия отстала от живого каталога.

- [ ] **2.4 28 871 программа есть в каталоге и нет у источника.**
      По 553 сверенным вузам. Часть законна — агрегатор показывает не весь
      ассортимент. 8 350 уже получили метку `catalog-only` в рабочей копии
      и ждут применения (см. 3.1).

- [ ] **2.5 Хвост СИТО — что уже опубликовано и не проверено.**
      - 214 карточек: офсайт обойдён не полностью (`sito_offsite_unverified`).
        У Абердина 107 программ с меткой «нет у источника» оставлены по твоему
        решению «без проверки не убирать». Проверять или снимать метку?
      - 67 карточек: программы сняты как несуществующие. У aivancity убрано 17.
        Откат — `sito-backup.json`. Подтвердить.
      - 41 откат недообхода: обход нашёл меньше четверти программ карточки.
      - 96 вузов с обрезанным сбором: у Абердина найдено 218 адресов, разобрано 160.
        Поднимать потолок?
      - 9 офсайтов не отдали ни одной страницы курса — нужен разбор под домен.

- [ ] **2.6 Карточки под вопросом.**
      29 карточек-не-вузов со снятой меткой партнёрства (показывать?),
      19 с устаревшей меткой, 10 «это не вуз», 4 дубля (`liu-brooklyn` и
      `long-island-university-brooklyn-direct-entry`), 3 вуза без курсов у Edvoy.

---

# Категория 3 — всё остальное

## Применить накопленное

- [ ] **3.1 Применить рабочую копию в живой каталог.** Самое крупное и опасное.
      Каталог вырастет на 14 820 программ (+16 %), метка `catalog-only` встанет
      ещё на 8 350, кампусов +37. Откат по трём бэкапам, схема проверена: 0 нарушений.
      *Варианты:* всё разом · только метки без новых программ · по странам ·
      **пробой на 20 вузах** (рекомендую).

- [ ] **3.2 Применить 1 842 уже принятых решения.** `resolved` 1093, `ignore` 423,
      `applied` 316, `update` 10. Решение в панели — ещё не изменение данных.

## Цены и валюта

- [ ] **3.3 ЗАМЕР СДЕЛАН 23.08 — решение владельца применять СПЛОШЬ НЕЛЬЗЯ.**
      Скрипт `scraper/kompas-qs-fee-basis.mjs`, отчёт `sources/kompas/QS-FEE-BASIS.md`,
      карта для применения `sources/kompas/qs-fee-basis-map.json`.
      1050 записей (вуз+сумма), 34 864 программы. Критерий — отношение суммы к
      верху годового диапазона (`yearlyFeesStated`):

      | Разряд | Записей | Программ | Вузов |
      |---|---:|---:|---:|
      | `annual` (ratio ≤ 1.15) — годовая цена | 940 | 32 816 | 469 |
      | `annual-loose` (1.15–1.8) — серая зона | 73 | 1 417 | 62 |
      | `whole-term` (ratio ≥ 1.8) — за весь срок | 35 | 615 | 27 |
      | `currency-mismatch` | 2 | 16 | 1 |

      **Кампусная сумма QS в 94 % случаев — годовая.** Kwantlen (ratio 3.9)
      оказался исключением, а не образцом. Подписать всё «за всю программу» —
      завысить 32 816 программ в 2–4 раза.
      Что делать: писать «за всю программу» только для разряда `whole-term`
      (частные колледжи — SRH, EU Business School, LCI, LaSalle, Drexel PG,
      ASU PG, Johns Hopkins, Kwantlen); остальное подписывать как годовую цену
      кампуса за уровень. `annual-loose` и 2 записи Hult (цена GBP/AED против
      диапазона USD) — оператору поштучно.
      Мусор в `whole-term`: Oxford Int. NA Pathway (диапазон 250–250, ratio 196)
      и PHBS (валюта `null`) — не применять, это хвосты 1.9/1.10.

      **ПРИМЕНЕНО 23.08** — `scraper/kompas-qs-fees-apply.mjs`, отчёт
      `sources/kompas/QS-FEES-APPLY.md`, откат `sources/kompas/qs-fees-apply-backup.json`.
      Записано **14 600 цен разряда `annual` в 262 карточки** рабочей копии
      (пустые места; существующие цены каталога не тронуты).
      `kompas-workcopy-check.mjs`: 1535 нарушений до и после — ни одного нового.
      Не записано: 10 119 `no-match` (программы выгрузки, которых нет в карточке —
      это хвост 1.9), 2 044 не-`annual`, 920 конфликт валюты карточки,
      232 валюты нет в схеме (MYR 175, SGD 111), 3 384 `fee-mismatch` (см. ниже).

- [x] **3.3-a СДЕЛАНО 23.08. Признак основы цены — попрограммный.**
      Схема: `program.tuitionBasis: 'year' | 'program'` (`site/src/schema/university.ts`).
      Годовой min/max теперь считает `annualTuitionValues()` из `site/src/lib/tuition.ts`,
      через неё ходят все семь мест, где раньше брались сырые `tuition.byProgram`:
      карточка каталога, сравнение, каталог, страница вуза (ru и /[lang]/), LandingBody.
      Строка программы подписывает такую сумму «за всю программу»
      (`lp.programs.perProgram`, ru/en/kk). Тест `site/src/lib/tuition.test.ts`.
      Данные: записано **56 цен whole-term в 15 карточек** рабочей копии
      (`kompas-qs-fees-apply.mjs`, разряд пишется вместе с `tuitionBasis`).
      Остальной whole-term не записан: 221 `no-match` (SRH 130 — программ нет в
      карточке, хвост 1.9), 42 валюта вне схемы (MYR у APU Malaysia), 8 конфликт
      валюты карточки, прочее — у программы уже есть цена каталога.
      Отсечка мусора: `ratio > 12` не пишется ни с какой подписью
      (Oxford Int. NA Pathway, диапазон 250–250).
      Проверено: `npm test` 24/24, `astro build` 2 668 страниц без ошибок,
      `kompas-workcopy-check` 1535 нарушений до и после — новых нет.
      Не проверено вживую: подпись «за всю программу» на странице вуза —
      в живом каталоге признака ещё нет, он появится после применения рабочей копии (3.1/3.2).

- [ ] ~~**3.3-a Разряд `whole-term` (615 программ, 27 вузов) — записать НЕЛЬЗЯ,**~~
      пока сайт подписывает любое число из `tuition.byProgram` как цену за год
      (`card.perYear` в `UniversityCardV2.astro`, min/max карточки берётся оттуда же).
      Нужен признак основы. Разряды внутри одной карточки смешаны (54 файла),
      значит признак нужен ПОПРОГРАММНЫЙ: `program.tuitionBasis: 'year' | 'program'`
      в `site/src/schema/university.ts` + подпись в карточке и на странице вуза
      + исключение таких сумм из min/max «от … в год». Список: `qs-fee-basis-map.json`,
      `basis: wholeTerm`.

- [ ] **3.3-b 3 384 `fee-mismatch` — у программы есть цена каталога, QS даёт другую.**
      Решение владельца 23.08 их НЕ закрывает: оно было принято в предположении,
      что кампусная сумма — цена за весь срок, а замер показал, что она годовая,
      то есть основа у обеих цен одна и это спор о значении, а не о подписи.
      Кто главнее — каталог или QS — отдельное решение. Список:
      `sources/kompas/qs-fees-apply-cases.json`, `reason: fee-mismatch`.
      Старая формулировка: **342 расхождения цены — один вопрос на все.** У QS цена не программная,
      а стоимость обучения кампуса за уровень (`feeBasis: campusLevelStated`).
      Прецедент 29.07 принимался, когда источники давали программную цену.
      *Варианты:* оставить цену каталога · писать кампусную как есть · писать,
      но подписывать «стоимость обучения на кампусе» · убрать цену вовсе.
      **От этого зависит весь остальной ценовой блок.**

- [ ] **3.4 1 986 кейсов `direct_fee_unlinked` у 132 вузов.** Самый крупный разряд
      панели. С офсайта снята сумма, но подпись рядом — не название программы.

- [ ] **3.5 132 расхождения валюты у 18 вузов.** Chester 23, Wollongong Dubai 20,
      Arden 19, Schiller 10, Middlesex Dubai 8, Reading Malaysia 8.

- [ ] **3.6 17 кейсов, где расходится не валюта, а сумма** (`kompas_fee_amount_fx`).
      Сверка их не видит по построению. Wollongong Dubai — 23 %.

- [ ] **3.7 10 кейсов «цена в чужой валюте».** Arizona State — 7 программ в GBP
      от Edvoy. Либо чужой кампус, либо ошибка атрибуции источника.

- [ ] **3.8 Переписать примечание к 271 кейсу `kompas_fee_absent`.** Решение
      (`ignore`) верное, но текст прецедента говорит «у источника цены нет»,
      а она есть, просто кампусная. Делать вместе с 3.3.
      После применения 23.08 у части этих программ цена уже проставлена —
      сверить список с `qs-fees-apply-report.json` перед переписыванием текста.

## Стипендии

- [x] **3.9 СДЕЛАНО 23.08.** Канадские и австралийские стипендии больше не
      показаны в долларах США. `scraper/kompas-scholarship-currency-fix.mjs`
      переносит сумму из пересобранной выгрузки в каталог: меняется только поле
      `amount` и только там, где каталог говорит USD, а выгрузка уже нет.
      Ничего не вычисляется — сумма берётся из выгрузки как есть.
      **Исправлено 202 суммы в 62 карточках: по 101 в рабочей копии и в живом
      каталоге** (`site/src/content/universities` — врал именно он; правка узкая
      и от 3.1 не зависит). Канада 128, Австралия 68, Новая Зеландия 4, ОАЭ 2.
      Отчёт `sources/kompas/SCHOLARSHIP-CURRENCY.md`, откат
      `sources/kompas/scholarship-currency-backup.json`.
      Проверено: гейт каталога — те же два pre-existing нарушения и ни одного
      нового, `kompas-workcopy-check` 1535 до и после, `npm test` 24/24.
      Осталось 20 сумм у 11 вузов (по 20 в каждой копии) — см. 3.9-a.
      Ошибка была в сборщике: до 10.08 он нормализовал голый «$» в «USD»,
      не глядя на страну. Сборщик починен, 323 из 341 выгрузки пересобраны.

- [ ] **3.9-a Хвост: 20 сумм у 11 вузов чинятся только пересбором с офсайта.**
      28 записей — в выгрузке та же USD (июльские сборы старым сборщиком),
      12 — в выгрузке нет стипендии с таким названием. Вузы: acap 8,
      herzing-college 6, rmit-university 6, southern-cross-university 6,
      manitoba 2, middlesex-dubai 2, ozyegin-university 2, pihms 2,
      swinburne 2, victoria-wellington 2, wollongong-malaysia 2.
      Локальных данных нет: нужен `kompas-collect-scholarships.mjs` по этим 11.
      Список: `sources/kompas/SCHOLARSHIP-CURRENCY.md`.

- [ ] **3.10 1 234 стипендии с неизвестным происхождением** (`untraceable`) —
      ни ссылки, ни источника. Показывать, прятать или удалять?

- [ ] **3.11 4 расхождения сумм + 3 одиночных кейса** (нет стипендии у источника,
      брак разбора, сводка). И 6 вузов вообще без стипендий.

## Полнота карточек

- [ ] **3.12 618 карточек без `officialUrl`** — на странице нет ссылки на сайт вуза.
      Wikidata их не знает; нужен платный поиск (Exa/WebSearch) или руки.

- [ ] **3.13 110 вузов без логотипа, 1 без фото** (`basis-mclean`).

- [ ] **3.14 57 недостающих кампусов + брак разбора кампусов у 6 коллекторов**
      (ema, englishpath, gbs-dubai, gbs-malta, global-banking-school, schiller —
      в поле кампусов приезжают абзацы описания программ).

- [ ] **3.15 6 карточек с городом не от источника + 6 заполненных с офсайта**
      (уровень и длительность выведены из названия, `confidence 0.4`).

## Перед публикацией

- [ ] **3.16 Мерж `feat/kompas-qs` в `main` = деплой.** 168 коммитов.
      Делать после того, как разберёшься с 3.1 и 3.3.

- [ ] **3.17 Заполнить `contacts.ts`** — реальные телефон, WhatsApp, Telegram,
      email, БИН, адрес. Сейчас заглушки.

- [ ] **3.18 Прописать `CRM_WEBHOOK_URL` в Cloudflare Pages.** Без него формы
      отдают ошибку и лиды не доходят. **Блокирует открытие сайта на трафик.**

- [ ] **3.19 Видео-отзывы** — залить на YouTube (unlisted), ссылки в
      `STUDYROOM_REVIEWS`. Секция сейчас `[TBD]`.

- [ ] **3.20 `og:image` 1200×630** — нужен дизайн-ассет.

- [ ] **3.21 Хардкод в блоке «Требования».** «GPA 4.0+» и «IELTS 6.0
      Pre-Master's / 6.5 магистратура» показываются всем странам, хотя это
      UK-pathway формулировка. Часть баг, часть твоё маркетинговое решение.

## Свежесть источников и техдолг

- [ ] **3.22 Обновить выгрузки агрегаторов.** QS собран 15.08, остальные —
      месяц назад: Edvoy 25.07, direct 31.07, IAPro 24.07, Kaplan / Oxford
      International / Study Group 22.07. **CATS — 0 файлов, источник пустой.**

- [ ] **3.23 IAPro: 11 партнёров не записаны нигде.** Документ говорит «11
      партнёров, вкладка Marketing Hub», но названий локально нет. Нужен ты
      или выгрузка. Блокирует разметку этих вузов.

- [ ] **3.24 Navitas UK отдаёт 404** — переехали. Затронуто 10 британских вузов.

- [ ] **3.25 «Безопасные» режимы пишут в данные** — флаги dry-run работают не
      так, как заявлено (найдено 20.07, не починено).

- [ ] **3.26 Починить `verdict: absent`** — по 24 записям стоял вердикт
      «карточки нет вовсе», хотя у семи карточка есть и никем не занята.
      Тот же класс, что даёт 2.2.

---

# Предлагаемый порядок

1. ~~**3.9** — стипендии в USD.~~ Сделано 23.08, остался хвост 3.9-a (пересбор 11 вузов).
2. ~~**3.3** — цена QS.~~ Замер, применение и признак основы сделаны 23.08;
   открыты 3.3-b (расхождения каталог/QS) и ценовые 3.4–3.8.
3. **1.7 + 2.2 одним заходом** — привязка QS с обеих сторон. Механическая часть сделана
   21.08 (64 выгрузки); остаток — четыре вопроса в `CAT1-REVIEW.md`.
4. **1.5** — дозавести шесть карточек, ждущих города. Дёшево. (1.6 закрыта 21.08.)
5. **3.1** — применение рабочей копии, пробоем на 20 вузах.
6. **2.5** — достоверность того, что уже опубликовано.
7. **3.17, 3.18** — без них сайт нельзя открывать на лиды.
8. **2.1** — 109 карточек без источника. Крупное решение, но не срочное.
9. **3.16** — мерж и деплой, когда предыдущее устоялось.

# Где что лежит

| Что | Файл |
|---|---|
| Решения владельца, все разряды | `sources/kompas/OWNER-DECISIONS.md` |
| Очередь заведения карточек | `sources/kompas/NEWCARDS-QUEUE.md` |
| Кейсы последнего заведения | `sources/kompas/newcards/build-cases.json` |
| Что заведено 20.08 (для отката) | `sources/kompas/newcards/applied.json` |
| Сверка «каталог против источника» | `sources/kompas/DIFF-REPORT.md` |
| Покрытие источниками | `sources/kompas/coverage-report.json` |
| Непривязанные записи QS | `sources/kompas/qs-unlinked.json` |
| История задач | `BACKLOG.md` |
