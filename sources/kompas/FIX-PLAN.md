# КОМПАС — план исправления ошибок каталога (сессия 5)

> **СТАТУС 2026-07-26 (сессия 6): хвост P3/P4 закрыт** (отчёт `OWNER-REPORT-session6.md`).
> Решения владельца выполнены: 6 пар дублей слиты объединением, непроверяемые стипендии
> помечены, а не сняты. Собранные с офсайтов стипендии перестали быть мёртвым грузом —
> сверены с каталогом, 553 кейса решений в панели. Разборщики сбора покрыты тестами,
> выборка `hub-headings` просмотрена глазами. Панель: 1894 → **2469** кейсов.
> **Единственное, что упирается в владельца, — доступ QS Apply** (227 вузов, 21% каталога).
>
> **СТАТУС 2026-07-25 (сессия 5):** P0–P2 закрыты (отчёт `OWNER-REPORT-session5.md`),
> решения владельца по CHF/ценам выполнены. **P3 и P4 разобраны** — отчёт
> `OWNER-REPORT-session5-p3p4.md`:
> - **P3.7** — упирается в владельца: QS Apply отклоняет оба пароля (проверено 2026-07-25).
>   227 вузов, 15 032 программы (21% каталога) остаются несверяемыми. Вход IAPro починен.
> - **P3.8** — «8 вузов, источник готов» оказалось ошибкой разметки: собрать нельзя ни
>   одного, у трёх источник отдаёт ноль, четыре — дубли карточек, один добран (ISM).
> - **P4.10** — панель показывает кейсы сессии 5; подписи issue дописаны.
> - **P4.9** — стипендии: замер показал, что 1234 из 1765 записей непроверяемы; написан
>   сбор с офсайтов (`kompas-collect-scholarships.mjs`), пилот на 25 вузах.

**Основание:** замер сессии 4 (`kompas-diff.mjs`, 2026-07-24). Сверено 462 вуза,
2112 кейсов. Работаем на копии `sources/kompas/catalog-work`, живой каталог не трогаем
до финальной подмены (правило 5). Ничего не удаляем и не выдумываем (правила 4).

## Что показал замер (факты, не ощущения)

- Партнёров 708, реально сверить удалось **462**. **227 — за логином (QS/IAPro), слепая зона.**
- Программ в каталоге 57 673, подтверждено источником **36 052 (62%)**; **21 621 (38%) — только в каталоге**.
- У источников есть 15 679 программ, которых нет в каталоге (недобор).
- Цена расходится >2%: 2 232. Валюта: 79 (см. оговорку). Цена у источника есть, в каталоге нет: 25 560.

## Оговорка по валюте (найдено глазами 2026-07-24)

«79 расхождений валюты» — **в основном ложная тревога**: у британских вузов (chester, arden)
каталог в GBP — это верно, а источник показывает пересчёт в USD/EUR. Валюта в схеме одна на вуз.
Реальные баги единичны (bhms: швейцарская школа в EUR вместо CHF). Массовый авто-фикс «по
источнику» ЗАПРЕЩЁН — сломает корректное. Каждый случай сверяем со страной вуза.

## Приоритеты

### P0 — недостоверность на живом сайте (закрыть раньше очереди, решение владельца)
1. **Фабрикованные цены Navitas** — 10 UK-вузов, сид-таблица `UK_FEE_BAND_BASE`. Занулить/пометить.
2. **Выдуманные программы QA** — `fallbackPrograms()`. Убрать из живого каталога.
3. **Сид-цены Oxford International** — литералы; заменить настоящими с источника (они собраны).
4. **Валюта, страна ≠ валюта** — точечно (bhms→CHF и подобные). Country→currency карта, НЕ по источнику.

### P1 — «только каталог» (21 621, подозрение на выдумку/устаревшее)
Разобрать по убыванию, начиная с монстров:
`chester` 724 · `swinburne` 541 · `victoria-wellington` 439 · `glasgow` 117 …
По каждому решить: источник прячет / устарело / выдумано. Не удалять — пометить `kompasStatus`.
Метод: партиями, глазами по образцам (не по цифре).

### P2 — цены и недобор
5. **2 232 расхождения цены** → в `/manager`, применять порог владельца >25% (как СОРОКА).
6. **Добить недобор** — 15 679 программ + 25 560 цен у источников есть, в каталоге нет. Чистый плюс.

### P3 — слепая зона (нужны доступы владельца)
7. **227 вузов за логином** — собрать QS Apply + IAPro, как только подтверждены пароли.
   → *2026-07-25: QS отклоняет оба пароля. IAPro работает, но заблокированных по нему нет
   (все 227 завязаны на QS). Ждём рабочий доступ.*
8. **8 вузов** — источник готов, выгрузки нет (дыры сбора), добрать.
   → *2026-07-25: разобрано. Добираемых нет: 3 — источник отдаёт ноль программ, 4 — дубли
   карточек (цель слияния теперь берётся у источника), 1 (ISM) добран точечным прогоном.*
   → *2026-07-26: **ЗАКРЫТО**. Все 6 пар дублей слиты объединением (решение владельца):
   fisher-college, ism-germany, liu-brooklyn, liu-post, tamucc, royal-holloway. Ничего не
   удалено — дубли помечены `mergedInto`, откат `dupmerge-backup.json`. Кандидатов в дубли
   по сканеру: **0**. `catalog-work` 823/823 валидны.*

### P4 — структурные дыры
9. **Стипендии** — не собирает никто. Единственный источник — офсайты. Отдельный сбор.
   → *2026-07-25: замер (`kompas-scholarship-audit.mjs`) — 1765 записей, у 0 есть source
   или verifiedBySite, 1234 непроверяемы, 346 карточек без стипендий вовсе. Сбор написан
   (`kompas-collect-scholarships.mjs`): офсайт → раздел → отдельные страницы стипендий.*
   → *2026-07-26: **ЗАКРЫТО**. (а) Разряд происхождения проставлен всем 1769 записям
   (`kompasStatus`: untraceable 1234, cloned 94, generic-external 85, linked 356), поля
   объявлены в `scholarshipSchema` сайта — иначе zod срезал бы метку; откат
   `scholarship-marks.json`. Ничего не удалено. (б) Собранное с офсайтов сведено с каталогом
   (`kompas-scholarship-diff.mjs`): 319 вузов, каталог 908 ↔ офсайт 1814, совпало 18;
   не подтвердилось 890, недобор 1796 — 553 кейса решений в панели. (в) Разборщики вынесены
   в `lib/kompas-scholarships-parse.mjs`, 26 тестов, проверены мутациями. (г) Выборка
   `hub-headings` просмотрена глазами: брака 8% против 5% у detail-pages, 131 запись
   отброшена (`kompas-scholarships-clean.mjs`, откат `scholarship-clean-backup.json`).*
10. **Панель `/manager`** пока не читает `diff-review.json` (2112 кейсов невидимы) — подключить wiring.
    → *2026-07-25: diff-review читался и раньше, невидимы были кейсы сессии 5 (5 файлов) —
    подключены. Дописано 20 подписей issue: оператор видел сырые слаги.*

## Порядок исполнения
P0 (быстро, однозначно) → P1 (объём, глазами) → P2 → P3 (по доступам) → P4.
Каждый шаг: правка в catalog-work → `validate-unis` → кейсы в панель → git-коммит-чекпойнт.

**Выполнено 2026-07-24 (сессия 5), скриптами:**
- P0.4 — валюта: авто-относок 0 (все 79 разобраны; bhms заблокирован enum схемы — CHF нет).
- P1 — помечено 21 621 catalog-only программ (`kompasStatus`), у 333 вузов, не удалено.
- P2 (п.5) — 110 расхождений цены >25% → `fee-mismatch-review.json`, авто-перезаписи нет.
- P2 (п.6) — добор: 24 515 цен + 13 885 программ; guard урока 10 отсёк 45 чуждо-валютных.
- Ядро замера вынесено в `lib/kompas-diff-core.mjs` (переиспользуют все скрипты выше).
