# Задачи на 2026-05-30 — studyroom-project-site

## 🔴 КРИТИЧНО

### 1. БОБР — исправить таймаут
- Добавить `--limit N` в `scraper/bobr.mjs` — не более N uni за прогон (default 50)
- В `bobr-on-demand.yml` добавить input `limit`
- Порядок: сначала unis из `shmel-worklist.json` (critical → high → medium)
- **Причина:** сейчас падает по таймауту (804 uni × 2 запроса = ~45 мин)

### 2. CATS — написать коллектор
- Создать `scraper/scrape-cats-all.mjs`
- Целевой сайт: https://www.catscolleges.com/
- Партнёры: Cambridge, Oxford, Exeter, UCL, Durham, Bristol, Warwick
- День 12 в `scrape-staggered.yml` уже зарезервирован — просто написать скрипт

---

## 🟡 ВАЖНО

### 3. РЕВИЗОР — дописать интеграцию
- `scraper/revizor.mjs` уже есть и работает (проверяет URL программ)
- Проверить что вывод `sources/revizor-flags.json` совпадает с форматом для `generate-verify.mjs`
- Запустить `revizor.mjs` → `generate-verify.mjs` → убедиться что `public/api/verify.json` корректный
- Кнопка "Проверить актуальность" в manager уже подключена к `verify-on-demand.yml`

### 4. Тест новых коллекторов
- `scrape-qahe-all.mjs` — запустить локально, проверить `sources/qahe-extracts/`
- `scrape-gedu-all.mjs` — проверить нужен ли Playwright
- `scrape-iapro-all.mjs` — требует `IAPRO_LOGIN` / `IAPRO_PASS` в `.env`

### 5. scrape-monthly.yml — упростить
- Сейчас запускает всё разом (90+ мин) → таймаут
- Упростить до: detect-gaps + merge + БАМБЛБИ --dry-run + build + deploy
- Тяжёлые коллекторы теперь покрывает `scrape-staggered.yml`

### 6. Проверить флаги `--slug` у ПАУК/БОБР/ОРЁЛ
- БАМБЛБИ вызывает `pauk.mjs --slug X`, `bobr.mjs --slug X`, `orel.mjs --slug X`
- Если флаги не поддерживаются — добавить в скрипты

---

## 🟢 МЕЛОЧИ

### 7. Manager UI — schedules в статусе
- `generate-status.mjs`: добавить чтение `aggregator-schedules.json` → в payload
- Manager: показать `lastRunAt` / `nextRunAt` для каждого агрегатора

### 8. Логотипы — расширить покрытие
- `scraper/discover-logos.mjs` → проверить сколько из 804 uni имеют лого
- `site/public/logos/` сейчас ~96 файлов

---

## 📋 Статус системы (конец 2026-05-29)

| Компонент | Статус |
|-----------|--------|
| ПАУК | ✅ on-demand workflow |
| БОБР | ⚠️ Таймаут — нужен --limit |
| ОРЁЛ | ✅ on-demand workflow |
| БАМБЛБИ | ✅ создан, не тестировался |
| РЕВИЗОР | ⚠️ скрипт есть, интеграция не завершена |
| Staggered schedule | ✅ 11 агрегаторов × каждые 3 дня |
| Manager UI | ✅ 804 unis, run selector modal |
| Catalog | ✅ 804 unis, динамические счётчики |
| CATS коллектор | ❌ не написан |

## 🗂 Ключевые файлы
```
.github/workflows/scrape-staggered.yml   ← стaggered расписание
scraper/sources/aggregator-schedules.json ← конфиг дней/интервалов
scraper/bumblebee.mjs                    ← БАМБЛБИ
scraper/revizor.mjs                      ← РЕВИЗОР (аудит)
site/scripts/generate-verify.mjs         ← генерирует verify.json
site/src/pages/manager.astro             ← панель управления
```
