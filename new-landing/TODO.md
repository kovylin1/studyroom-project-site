# TODO для new-landing

Бриф от пользователя — что доделать в `new-landing/[slug].astro` перед раскаткой на все 27 страниц.

## 1. Sticky bottom CTA с чат-окном (как на текущем prod)

В новом шаблоне сейчас два sticky-элемента: зелёная кнопка WhatsApp справа снизу (`.sticky-wa`) и карточка-плашка «Айгерим» слева снизу (`.chat-fab` → разворачивается в `.chat-panel`).

**Заменить на схему текущего prod:**
- Убрать `.sticky-wa` (зелёный WhatsApp справа)
- Убрать `.chat-fab` слева снизу
- Перенести с текущего prod `site/src/pages/[slug].astro` (откат к старому шаблону уже сделан) sticky-кнопку «вызов консультанта» снизу + всплывающее окно чата при клике
- Поведение: одна кнопка снизу → открывается окно чата прямо там же (не лайт-режим, без перекидывания в WhatsApp)

## 2. Убрать любое упоминание Kaplan в стипендиях

Клиенту знать про партнёров не нужно.

- В блоке money / `.scholar` карточках:
  - «Стипендия Kaplan за раннюю подачу» → «Стипендия за раннюю подачу»
  - «Стипендия Kaplan за академические достижения» → «Академическая стипендия»
  - Аналогично — в модалке детали стипендии
- Также проверить chat quick replies (`QR.scholarships` в `<script>`) — там тоже Kaplan упоминается
- **Где править:** либо preprocessing в frontmatter `[slug].astro` (replace `/Kaplan\s*/gi`), либо обновить source data в `site/src/content/universities/*.json` и `scraper/src/sources/uni-scholarships.ts` (последнее — основательно)
- Решить: гасить только при рендере (быстро, реверсивно) или править данные на источнике (долговременно)

## 3. Верхнее меню — объединить и упростить

- **Убрать** кнопку «Консультация» из правой части `.lp-nav` (sticky bottom уже её закрывает)
- **Убрать** ссылку на `studyroom.kz` из `site/src/layouts/Base.astro` (раздел `.site-header__nav`)
- **«Каталог»** перенести в само меню как основной пункт (первым, после бренда)
- Меню должно **прилипать сверху при скролле** — сейчас `position: sticky` уже стоит, проверить, что не отваливается на мобильном и что не перекрывается другим элементом

## 4. Программы — фильтр уровня + кликабельные карточки

Сейчас в секции `#programs`:
- Табы по факультетам
- Список из 6 программ
- Кнопка «Показать все программы факультета» → модалка со всеми программами

**Добавить:**
- Фильтр уровня: `Все / Foundation / Бакалавриат / Pre-Master's / Магистратура`
- Каждая карточка программы кликабельная: клик → открывает модалку со ВСЕМИ программами **этого факультета + этого уровня** (faculty + level pre-filtered)
- Пример: пользователь видит «BSc Mechanical Engineering» (бакалавр, Engineering) → клик → модалка «Все программы Бакалавриат · Engineering» с поиском внутри
- В модалке тоже должен работать фильтр уровня, чтобы можно было переключить между Бакалавриат / Магистратура и т.д. внутри одного факультета

## 5. Убрать секцию `#photos` ниже на странице

Photo strip под hero достаточен.

- Удалить весь блок `{galleryItems.length > 0 && (<section id="photos">...)}` (там сейчас 4-категории фото или дефолтная сетка)
- Соответственно убрать ссылку «Фото» из drawer-меню (мобильное меню)

---

## Где что лежит

- Новый шаблон: `new-landing/[slug].astro`
- Standalone-прототип: `new-landing/glasgow-v2.html` (для референса)
- Текущий prod-шаблон (старый, для копирования sticky-чата): `site/src/pages/[slug].astro`
- Preview новой версии онлайн: <https://studyroom-redesign.pages.dev/glasgow>

## План шагов на завтра

1. Прочитать прод-шаблон, выдрать оттуда sticky-кнопку + chat-модал, перенести в `new-landing/[slug].astro` вместо текущих sticky-wa + chat-fab.
2. Заменить «Kaplan ...» на чистые названия в money-секции, чате и модалке.
3. Подчистить шапку: убрать «Консультация» + `studyroom.kz`, добавить «Каталог» первым пунктом меню.
4. Добавить фильтр уровня в `#programs`, сделать карточки кликабельными → модалка с predefined faculty + level фильтром.
5. Удалить секцию `#photos` + ссылку на неё из drawer.
6. `rm -rf site/.astro && npm run build` + деплой на тот же preview-проект `studyroom-redesign` для финальной проверки.
7. После одобрения — скопировать `new-landing/[slug].astro` → `site/src/pages/[slug].astro`, билд, деплой на основной `studyroom-project-site`.
